version: 2.2
 runtime-versions:
      java: openjdk8
      dotnet: 2.2
phases:
  pre_build:
    commands:
      - dotnet restore
  build:
    commands:
      - dotnet build
artifacts:
  files:
    - bin/Debug/netcoreapp1.1/*
  discard-paths: yes
